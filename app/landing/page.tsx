import { LandingPageApp } from "@/components/landing-page/LandingPageApp";

export const metadata = {
  title: "Flowstate — Spatial thinking canvas",
  description:
    "A spatial thinking canvas for AI-assisted inquiry. Branch parallel threads, keep context, and think in space—not in a list.",
};

export default function LandingPage() {
  return <LandingPageApp />;
}
